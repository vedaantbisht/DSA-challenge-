//3Sum

class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        set<vector<int>> val;
        int num = nums.size();

        for(int i = 0; i<num; i++){
            for(int j = i +1; j<num; j++){
                for(int k = j+1; k<num; k++){

                    if(nums[i]+ nums[j]+ nums[k]==0){
                        vector<int> temp = {nums[i], nums[j], nums[k]};
                        sort(temp.begin(), temp.end());
                        val.insert(temp);
                    }
                }
            }
        }

        return vector<vector<int>>(val.begin(), val.end());
    }
};

/*
Input
nums =
[-1,0,1,2,-1,-4]
Output
[[-1,-1,2],[-1,0,1]]
Expected
[[-1,-1,2],[-1,0,1]]
*/
